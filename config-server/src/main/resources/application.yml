server:
  port: 8888

logging:
  level:
    root: INFO
    org.spring.framework.cloud.config: DEBUG

spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: file://Users/bharatverma/code/personal/java/config-server-repository
          default-label : master
          clone-on-start: true
    fail-fast: true
